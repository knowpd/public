apiVersion: v1
kind: Service
metadata:
  name: {{ include "grfcl-alloy-v3.fullname" . }}
  labels:
    {{- include "grfcl-alloy-v3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grfcl-alloy-v3.selectorLabels" . | nindent 4 }}
